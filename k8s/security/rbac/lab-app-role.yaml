apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: lab-app-viewer
  namespace: lab-app
rules:
  # Permissões mínimas para ler pods no próprio namespace.
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
