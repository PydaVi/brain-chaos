apiVersion: v1
kind: LimitRange
metadata:
  name: default-limits
  namespace: lab-app
spec:
  limits:
    - default:
        cpu: 300m
        memory: 256Mi
      defaultRequest:
        cpu: 100m
        memory: 128Mi
      type: Container
