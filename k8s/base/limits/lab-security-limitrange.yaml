apiVersion: v1
kind: LimitRange
metadata:
  name: default-limits
  namespace: lab-security
spec:
  limits:
    - type: Container
      default:
        cpu: 300m
        memory: 256Mi
      defaultRequest:
        cpu: 100m
        memory: 128Mi
